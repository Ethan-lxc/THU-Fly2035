---
description: 游戏内对白「按 Space 继续」提示、延迟与闪烁的统一规范
alwaysApply: true
---

# 对话：逐句继续提示（按 Space 继续）

## 产品体验（必须对齐）

- **每一句**台词在打字结束（或玩家按空格瞬间出全句）后，**下方**须给出继续提示，默认文案：**「按 Space 继续」**（可用同一套 TMP 控件，如 `choicePromptText`）。
- 提示**不要出现得太快**：须在**本句结束**后再经过一段 **unscaled 延迟**（建议约 **0.5～0.8s** 以上，避免与打字机抢注意力），再显示提示。
- 提示须有**可见的闪烁**：例如对文字 **Alpha** 做 **PingPong**（周期约 **1～1.5s**），并设**透明度下限**（约 **0.35～0.45**），避免刺眼或难以辨认。

## 实现落点（本仓库）

- **`GameplayDialoguePanel`** 内置对白（`BeginSequence` 医院线、`BeginNarrativeSequence` 等）已在 Inspector 中提供 **`narrativeContinuePrompt*`** 系列字段（文案、延迟、闪烁开关、周期、Alpha 下限）。**新增或修改内置逐句对白时**，须保持上述行为，**不要**改回「无提示 / 立即出现 / 无闪烁」。
- **抉择阶段**（如医院任务接受/拒绝）使用**另一套**提示文案（如 `acceptPromptText`），与「逐句继续」**分开**；进入抉择前须恢复提示文字基准色，避免闪烁残留。

## 外接对白（ExternalSession / 各 *NpcController）

- 不经过 `GameplayDialoguePanel` 内置 `Update` 对白循环时，须在**每句结束**后**同样**满足：延迟 + 闪烁 +「按 Space 继续」（或等价交互），**不得**只写正文而不给继续提示。
- 若复用 `choicePromptText` / `ExternalSetChoicePrompt`，**自行**实现与 `GameplayDialoguePanel` 一致的延迟与闪烁节奏，或抽取**公共工具**避免重复与不一致。

## 修改时禁止

- 为「省事」把逐句提示改成**整段结束后才出现一次**（除非策划明确只要单次提示）。
- 把延迟调到 **0** 或极短导致提示与打字机**同时抢屏**。
- 去掉闪烁且**无**其它高亮手段（除非全项目统一改版）。
