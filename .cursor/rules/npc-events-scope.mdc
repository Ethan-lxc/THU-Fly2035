---
description: 未经明确授权不得修改其他 NPC 事件相关代码或资源
alwaysApply: true
---

# NPC 事件修改范围

- **未经用户在本对话中明确允许**，不得修改与**当前正在处理的任务/NPC 无关**的「其他 NPC 事件」内容。
- 「其他 NPC 事件」包括但不限于：医院取药、身体不适同学、指路事件、通用 `QuestNpcInteractable` / `HospitalFetchMedicineEvent`、其他分支对白控制器、及其场景/预制体/配置引用；以及为上述事件服务的共享脚本，若改动会影响多条事件的行为，必须先征得用户同意。
- **允许**：仅改用户点名的文件或当前需求直接相关的脚本（例如只改找自行车专用控制器及其引用链）。
- **禁止**：为修一条支线而去「顺带」重构、重命名或改逻辑到其它 NPC 事件脚本，除非用户明确说可以动那些部分。
- 若完成目标必须触碰共享代码，应先**说明原因并请求确认**，或给出**最小绕行方案**（如新文件、新组件，而不改现有事件体）。
